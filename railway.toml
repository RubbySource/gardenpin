[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npm run build-frontend"

[deploy]
startCommand = "cd backend && node server.js"
healthcheckPath = "/api/gardens"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
